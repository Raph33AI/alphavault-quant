# ============================================================
# ALPHAVAULT QUANT — Python Dependencies
# ============================================================

# ── Core Data Science ──────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# ── Machine Learning ──────────────────────────────────────
xgboost>=2.0.0
lightgbm>=4.1.0
scikit-learn>=1.3.0
joblib>=1.3.0

# ── Deep Learning (optionnel, pour LSTM/CNN) ──────────────
# torch>=2.1.0           # Trop lourd pour GitHub Actions free tier
# tensorflow>=2.14.0     # Idem

# ── Statistics / Econometrics ─────────────────────────────
statsmodels>=0.14.0
arch>=6.2.0              # GARCH models

# ── Portfolio Optimization ────────────────────────────────
cvxpy>=1.4.0             # Convex optimization (MVO, risk parity)
pypfopt>=1.5.5           # PyPortfolioOpt

# ── Options Analytics ─────────────────────────────────────
py_vollib>=1.0.1         # Black-Scholes, Greeks
mibian>=0.1.3            # Options pricing

# ── Broker (Interactive Brokers) ──────────────────────────
ib_insync>=0.9.86        # IBKR API async

# ── HTTP Client ───────────────────────────────────────────
requests>=2.31.0
httpx>=0.25.0            # Async HTTP

# ── Async / IO ────────────────────────────────────────────
asyncio-throttle>=1.0.2
aiohttp>=3.9.0

# ── Data Serialization ────────────────────────────────────
orjson>=3.9.0            # JSON ultra-rapide

# ── Utilities ─────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.5.0          # Settings validation
pydantic-settings>=2.1.0
loguru>=0.7.2            # Logging avancé
tenacity>=8.2.3          # Retry logic

# ── Date/Time ─────────────────────────────────────────────
pytz>=2023.3
python-dateutil>=2.8.2
pandas-market-calendars>=4.3.0  # Calendriers de marché